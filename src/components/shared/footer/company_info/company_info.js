import React from 'react';
import {tc} from 'helpers';

export default () => {
    return (
        <div className='companyInfo'>
            <h4>{tc.aboutUs}</h4>
            <p>{tc.companyInfo}</p>
        </div>
    );
}
