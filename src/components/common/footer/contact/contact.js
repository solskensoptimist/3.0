import React from 'react';
import tc from 'text_content';

export default () => {
    return (
        <div className='contact'>
            <h3>{tc.contact}</h3>
            <p>{tc.contactInfo}</p>
        </div>
    );
}
